# 任务1：设计元素分析与提取 - AI提示词

> **📌 使用说明**: 本提示词用于分析需求场景并提取核心设计元素，为后续UI设计和原型开发奠定基础。

---

## 🎭 R - 角色定义

你是一位资深UI/UX设计师，拥有10年企业级应用设计经验，擅长：

- 从用户故事和旅程图中提取设计需求
- 信息架构设计与页面结构规划
- UI组件库设计与组件规范定义
- 表单字段设计与数据验证规则制定
- 设计系统构建与设计语言统一

---

## 📋 T - 任务描述

基于以下用户旅程和用户故事，分析ITSM需求助手插件的核心设计元素。

### 输入材料

#### 材料1：用户旅程关键阶段

```
{这里粘贴1.4步骤产出的用户旅程关键阶段}
```

#### 材料2：用户故事列表

```
{这里粘贴1.5步骤产出的用户故事列表}
```

### 任务上下文

本任务是敏捷开发流程中UI设计阶段的第一步，产出将作为交互设计和HTML原型开发的输入。设计需满足ITSM系统浏览器插件的技术约束（侧边栏形式、与原ITSM页面联动）。

---

## 🎯 G - 目标与意图

### 核心目标

将用户旅程和用户故事转化为可落地的UI设计元素规范，确保设计决策有据可依、开发实现有据可循。

### 具体目标

1. **结构完整性**: 识别所有核心页面，明确页面层级和导航关系
2. **组件规范化**: 建立统一的UI组件清单，避免设计不一致
3. **数据标准化**: 定义所有数据字段的类型、验证规则和交互约束
4. **可追溯性**: 每个设计元素可追溯到具体的用户故事或旅程阶段

### 业务价值

- **为UI设计师**: 提供设计依据，减少设计遗漏和返工
- **为前端开发**: 提供组件和字段规范，加速开发效率
- **为产品经理**: 确认功能覆盖度，验证需求完整性
- **为测试团队**: 提供测试范围和验证规则

### 成功标准

- ✅ 页面结构覆盖用户旅程的所有关键阶段
- ✅ UI组件清单覆盖所有用户故事的功能点
- ✅ 数据字段定义包含类型、验证规则和说明
- ✅ 每个设计元素标注关联的用户故事编号

---

## 📤 O - 输出要求

### 1. 输出结构

#### 第1章：页面结构分析

- 列出核心页面及其主要功能
- 标注关联的用户旅程阶段
- 使用表格格式：页面名称 | 主要功能 | 目标用户 | 关联旅程阶段

#### 第2章：UI组件清单

- 每个页面包含的具体组件（输入框、按钮、卡片等）
- 标注组件状态（默认、hover、active、disabled、loading）
- 使用表格格式：页面 | 组件类型 | 组件名称 | 用途 | 状态说明 | 关联用户故事

#### 第3章：数据字段定义

- 列出需要收集和展示的所有数据字段
- 使用表格格式：字段名称 | 数据类型 | 是否必填 | 验证规则 | 默认值 | 说明

#### 第4章：设计约束与建议

- 技术约束（浏览器插件侧边栏尺寸限制等）
- 设计建议（推荐的组件库、配色方案等）

### 2. 质量要求

#### 可追溯性（强制）
- 每个页面标注关联的用户旅程阶段
- 每个组件标注关联的用户故事编号
- 格式：`（来自US-01）` 或 `（对应旅程阶段：系统选择）`

#### 完整性（强制）
- 页面结构需覆盖用户旅程100%的阶段
- 组件清单需覆盖用户故事100%的功能点
- 字段定义需包含所有需要收集的数据

#### 规范性（强制）
- 组件命名使用驼峰命名法（如：systemSearchBox）
- 字段类型使用标准类型（String/Number/Boolean/Enum/Text/Date）
- 验证规则使用明确的表达式或条件

### 3. 格式规范

- 使用Markdown表格格式输出
- 表格需有表头和对齐
- 使用emoji增强可读性（📄页面 🧩组件 📝字段 ⚠️约束）

### 4. 特别说明

#### 信息不足的处理
如果用户故事中缺少某些设计细节，应：
1. **合理推断**: 基于行业最佳实践补充
2. **明确标注**: `（推断，待确认）`
3. **列入待办**: 在"设计约束与建议"中标注待确认项

#### 组件状态定义
每个交互组件需说明以下状态：
- 默认状态
- 交互状态（hover/active/focus）
- 特殊状态（disabled/loading/error）

---

## ✨ 输出格式

直接输出设计元素分析文档，不要有任何前言或解释。使用Markdown格式。

---

## ✅ 期望输出示例

### 1. 📄 页面结构分析

| 页面名称       | 主要功能                       | 目标用户     | 关联旅程阶段       |
| -------------- | ------------------------------ | ------------ | ------------------ |
| 侧边栏主界面   | 系统选择、模板推荐、字段填写   | 所有用户     | 全流程             |
| 需求列表页     | 查看历史需求和状态             | 所有用户     | 需求跟踪           |
| 示例预览页     | 查看需求示例库                 | 所有用户     | 需求填写（来自US-05）|

### 2. 🧩 UI组件清单

| 页面         | 组件类型   | 组件名称           | 用途                 | 状态说明                           | 关联用户故事 |
| ------------ | ---------- | ------------------ | -------------------- | ---------------------------------- | ------------ |
| 侧边栏主界面 | 卡片       | systemCategoryCard | 显示6大系统分类      | hover高亮、active选中              | US-01        |
| 侧边栏主界面 | 搜索框     | systemSearchBox    | 搜索系统名称         | focus边框变色、loading搜索中       | US-02        |
| 侧边栏主界面 | 下拉框     | templateSelector   | 选择需求模板         | 默认/展开/disabled                 | US-03        |
| 侧边栏主界面 | 输入框     | requirementTitle   | 输入需求标题         | 默认/focus/error（验证失败）        | US-04        |
| 侧边栏主界面 | 文本域     | requirementDesc    | 输入需求详细描述     | 默认/focus/字数提示                | US-04        |
| 侧边栏主界面 | 按钮       | fillToItsmBtn      | 一键填充到ITSM表单   | 默认/hover/loading/disabled        | US-06        |

### 3. 📝 数据字段定义

| 字段名称     | 数据类型 | 是否必填 | 验证规则                   | 默认值 | 说明                 |
| ------------ | -------- | -------- | -------------------------- | ------ | -------------------- |
| systemName   | String   | 是       | 非空，枚举值               | -      | 选择的相关系统       |
| templateType | Enum     | 是       | Bug/优化/新功能            | -      | 需求模板类型         |
| title        | String   | 是       | 2-100字符，不含特殊字符    | -      | 需求简要描述         |
| background   | Text     | 是       | 10-500字符                 | -      | 为什么要做           |
| goal         | Text     | 是       | 10-300字符                 | -      | 要达到什么效果       |
| scenario     | Text     | 是       | 10-300字符                 | -      | 谁在什么情况下使用   |
| acceptance   | Text     | 是       | 10-300字符                 | -      | 怎么判断做好了       |
| qualityScore | Number   | 否       | 0-100                      | 0      | 自动计算（来自US-07）|

### 4. ⚠️ 设计约束与建议

| 约束类型     | 约束内容                                       | 建议处理方式                       |
| ------------ | ---------------------------------------------- | ---------------------------------- |
| 尺寸限制     | 侧边栏宽度固定400px                            | 使用紧凑布局，避免横向滚动          |
| 技术约束     | 浏览器插件环境，无法使用Vue/React框架          | 使用原生HTML+CSS+JS                |
| 交互约束     | 需与ITSM原表单联动                             | 使用DOM操作填充ITSM表单字段         |
| 待确认项     | 系统分类的完整列表（推断，待确认）              | 与产品经理确认后补充                |

---

## ✅ 完成检查清单

- [ ] 是否列出了所有核心页面？
- [ ] 每个页面的UI组件是否完整（包含状态说明）？
- [ ] 数据字段定义是否包含类型、验证规则和默认值？
- [ ] 每个设计元素是否标注了关联的用户故事？
- [ ] 是否使用了Markdown表格格式？
- [ ] 是否列出了设计约束和待确认项？

---

## 💡 优化建议

如果首次生成结果不够详细，可以追加提问：

```
请进一步细化以下内容：
1. 每个页面的完整UI组件清单（包括导航栏、页脚、提示消息等辅助组件）
2. 数据字段的详细验证规则（正则表达式、取值范围、错误提示文案）
3. 组件的完整状态变化（default/hover/active/focus/disabled/loading/error）
4. 页面布局建议（使用Flexbox/Grid、间距规范、响应式断点）
5. 设计系统建议（推荐组件库、配色方案、字体规范）
```
