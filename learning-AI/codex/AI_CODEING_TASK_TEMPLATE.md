# AI_CODEING 任务提交模板

## 1. 通用模板

```text
任务名称：

背景：

目标：

问题现象 / 需求说明：

涉及范围：
- 模块：
- 文件：
- 页面 / 接口：

明确限制：
- 不要改：
- 可以改：
- 是否允许改接口 / 配置 / 数据结构：

期望动作：
- 是否先分析：
- 是否直接改代码：
- 是否需要编译：
- 是否需要测试：
- 是否需要写文档：

验收标准：
1.
2.
3.

输出要求：
- 文档写到：
- 代码改动范围：
- 最终说明需要包含：
```

## 2. 缺陷修复模板

```text
任务名称：修复 xxx 问题

现象：
1.
2.

复现步骤：
1.
2.
3.

怀疑范围：
1.
2.

限制：
1. 不要改外部接口。
2. 不要删除现有兼容逻辑。

要求：
1. 先定位根因。
2. 再直接改代码。
3. 编译验证。
4. 生成分析文档到 app.bak/z-readme。

验收：
1. 问题不能再复现。
2. 编译通过。
3. 输出修改说明和验证步骤。
```

## 3. 新功能模板

```text
任务名称：新增 xxx 功能

背景：

功能目标：

输入输出：

影响范围：

兼容性要求：

限制：

要求：
1. 按现有项目风格实现。
2. 不引入新三方库。
3. 补必要文档。

验收：
1. 功能可用。
2. 老功能不受影响。
3. 可编译、可验证。
```

## 4. 纯分析模板

```text
任务名称：分析 xxx 机制

目标：

范围：

重点回答：
1.
2.
3.

限制：
1. 先不要改代码。

输出：
1. 写文档到指定目录。
2. 包含结论、证据、风险、建议。
```

## 5. 推荐写法

推荐：

```text
帮我分析 app.bak/component/webserver 中设备参数导入的提交链路，确认前端到后端的参数编码方式，再写一份文档到 app.bak/z-readme。先分析，不要改代码。
```

不推荐：

```text
看看这里哪里有问题，顺便都优化一下。
```
